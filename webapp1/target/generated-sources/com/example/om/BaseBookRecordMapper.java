package com.example.om;

import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.HashSet;
import java.util.List;
import java.util.Set;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.apache.torque.Column;
import org.apache.torque.TorqueException;
import org.apache.torque.criteria.Criteria;
import org.apache.torque.om.mapper.RecordMapper;
import org.apache.torque.om.mapper.MappingStrategy;


/**
 * Maps ResultSet rows into Book objects.
 *
 * The skeleton for this class was autogenerated by Torque on:
 *
 * [Thu Jan 22 06:00:13 SGT 2026]
 *
 */
@SuppressWarnings("unused")
public class BaseBookRecordMapper implements RecordMapper<Book>
{
    /** Serial version */
    private static final long serialVersionUID = 1769032813872L;

    /** The class log. */
    private static Log log
            = LogFactory.getLog(BaseBookRecordMapper.class);
            
    private static final String BOOK_ID_EXPRESSION = BaseBookPeer.BOOK_ID.getSqlExpression();
    private static final String AUTH_ID_EXPRESSION = BaseBookPeer.AUTH_ID.getSqlExpression();
    private static final String TITLE_EXPRESSION = BaseBookPeer.TITLE.getSqlExpression();
    private static final String SUBJECT_EXPRESSION = BaseBookPeer.SUBJECT.getSqlExpression();

    private boolean useMappingStrategy = true;

    private MappingStrategy<Book> strategy;
    
    public void initStrategy()
    {
        this.strategy = new MappingStrategy<Book>();
    }
    

    /**
     * Constructs the object from the current row in the resultSet.
     *
     * @param resultSet the resultSet to operate on, already pointing
     *        to the correct row. Not null.
     * @param offset a possible offset in the columns to be considered
     *        (if previous columns contain other objects),
     *        or 0 for no offset.
     * @param criteria The criteria which created the result set.
     *        If set, the attributes to set in the data object
     *        are determined from the select columns in the criteria;
     *        if no matching column can be found, null is returned.
     *        If not set, all of the table's columns are read from the
     *        result set in the order defined in the table definition.
     *
     * @return the mapped object, not null.
     *
     * @throws TorqueException when reading fields from the RecordSet fails
     *         or if a Criteria is passed which contains select columns other
     *         than the columns in the BOOK table.
     */
    public Book processRow(
                ResultSet resultSet,
                int offset,
                Criteria criteria)
            throws TorqueException
    {
        Book book = new Book();

        try 
        {
            book.setLoading(true);
            if (criteria == null)
            {
                book.setBookId(
                        getBookId(resultSet, offset + 1));
                book.setAuthId(
                        getAuthId(resultSet, offset + 2));
                book.setTitle(
                        getTitle(resultSet, offset + 3));
                book.setSubject(
                        getSubject(resultSet, offset + 4));
            }
            else
            {
                if (useMappingStrategy) {
                    initStrategy();
                }
                // try to get columns to be mapped
                // from criteria's select columns
                int totalOffset = offset + 1;
                List<Column> selectColumns = criteria.getSelectColumns();
                List<Column> columnsWithoutOffset = selectColumns.subList(
                        offset, 
                        selectColumns.size());
                Set<String> columnsMapped = new HashSet<String>();
                for (Column column : columnsWithoutOffset)
                {
                    final int nextOffset = totalOffset;                     if (!columnsMapped.contains(BOOK_ID_EXPRESSION ) && BOOK_ID_EXPRESSION.equals(
                            column.getSqlExpression()))
                    {
                        if (useMappingStrategy) 
                        {
                            strategy.addColumn(nextOffset, 
                                (res, inst) -> inst.setBookId( getBookId(res, nextOffset)));
                        } else
                        {
                            book.setBookId( getBookId(resultSet, totalOffset));
                        }
                        columnsMapped.add( BOOK_ID_EXPRESSION );
                    }
                    else if (!columnsMapped.contains(AUTH_ID_EXPRESSION ) && AUTH_ID_EXPRESSION.equals(
                            column.getSqlExpression()))
                    {
                        if (useMappingStrategy) 
                        {
                            strategy.addColumn(nextOffset, 
                                (res, inst) -> inst.setAuthId( getAuthId(res, nextOffset)));
                        } else
                        {
                            book.setAuthId( getAuthId(resultSet, totalOffset));
                        }
                        columnsMapped.add( AUTH_ID_EXPRESSION );
                    }
                    else if (!columnsMapped.contains(TITLE_EXPRESSION ) && TITLE_EXPRESSION.equals(
                            column.getSqlExpression()))
                    {
                        if (useMappingStrategy) 
                        {
                            strategy.addColumn(nextOffset, 
                                (res, inst) -> inst.setTitle( getTitle(res, nextOffset)));
                        } else
                        {
                            book.setTitle( getTitle(resultSet, totalOffset));
                        }
                        columnsMapped.add( TITLE_EXPRESSION );
                    }
                    else if (!columnsMapped.contains(SUBJECT_EXPRESSION ) && SUBJECT_EXPRESSION.equals(
                            column.getSqlExpression()))
                    {
                        if (useMappingStrategy) 
                        {
                            strategy.addColumn(nextOffset, 
                                (res, inst) -> inst.setSubject( getSubject(res, nextOffset)));
                        } else
                        {
                            book.setSubject( getSubject(resultSet, totalOffset));
                        }
                        columnsMapped.add( SUBJECT_EXPRESSION );
                    }
                    totalOffset++;
                }
                if (columnsMapped.isEmpty())
                {
                    log.debug("no columns to map found in criteria, "
                        + "returning null");
                    return null;
                }
                if (useMappingStrategy)
                {
                  this.strategy.finish(4, true);
                  this.strategy.execute(resultSet, book);
                }
            }
            book.setNew(false);
            book.setModified(false);
        }
        finally
        {
            book.setLoading(false);
        }
        return book;
    }

    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field bookId in Book.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected int getBookId(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            return resultSet.getInt(columnIndex);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field authId in Book.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected int getAuthId(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            return resultSet.getInt(columnIndex);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field title in Book.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected String getTitle(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            return resultSet.getString(columnIndex);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field subject in Book.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected String getSubject(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            return resultSet.getString(columnIndex);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }

}

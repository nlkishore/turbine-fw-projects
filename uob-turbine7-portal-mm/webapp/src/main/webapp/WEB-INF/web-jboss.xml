<?xml version="1.0" encoding="UTF-8"?>
<!--
    JBoss 8 (WildFly) specific web.xml configuration
    This file is used when building with -Pjboss8 profile
-->
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee 
         https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">
    
    <display-name>uob-portal-mm-jboss</display-name>
    <description>UOB Turbine 7 Portal - JBoss 8 Deployment</description>

    <!-- Spring DispatcherServlet for REST APIs -->
    <servlet>
        <servlet-name>springDispatcher</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <init-param>
            <param-name>contextConfigLocation</param-name>
            <param-value>classpath:spring-config.xml</param-value>
        </init-param>
        <load-on-startup>1</load-on-startup>
    </servlet>
    <servlet-mapping>
        <servlet-name>springDispatcher</servlet-name>
        <url-pattern>/api/*</url-pattern>
    </servlet-mapping>

    <!-- Turbine Servlet -->
    <servlet>
        <servlet-name>action</servlet-name>
        <servlet-class>org.apache.turbine.modules.ActionLoader</servlet-class>
        <load-on-startup>2</load-on-startup>
    </servlet>
    <servlet-mapping>
        <servlet-name>action</servlet-name>
        <url-pattern>*.do</url-pattern>
    </servlet-mapping>

    <!-- Log4j2 Servlet Filter -->
    <filter>
        <filter-name>log4jServletFilter</filter-name>
        <filter-class>org.apache.logging.log4j.web.Log4jServletFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>log4jServletFilter</filter-name>
        <url-pattern>/*</url-pattern>
        <dispatcher>REQUEST</dispatcher>
        <dispatcher>FORWARD</dispatcher>
        <dispatcher>INCLUDE</dispatcher>
        <dispatcher>ERROR</dispatcher>
    </filter-mapping>

    <!-- Welcome files -->
    <welcome-file-list>
        <welcome-file>index.vm</welcome-file>
        <welcome-file>index.html</welcome-file>
    </welcome-file-list>

    <!-- JBoss 8 specific: Use Jakarta EE 10 APIs -->
    <metadata-complete>false</metadata-complete>
</web-app>

package com.uob.om;

import java.sql.Connection;
import java.util.List;

import org.apache.torque.TorqueException;
import org.apache.torque.criteria.Criteria;
import com.uob.om.TurbineUserGroupRole;

/**
 * The skeleton for this class was autogenerated by Torque on:
 *
 * [Thu Jan 22 21:23:26 SGT 2026]
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 */

public  class GtpRole
    extends com.uob.om.BaseGtpRole
    implements org.apache.fulcrum.security.model.turbine.entity.TurbineRole
{
    /** Serial version */
    private static final long serialVersionUID = 1769088206884L;

    /**
     * Override to use GtpRolePermissionPeer instead of TurbineRolePermissionPeer
     * This ensures queries use GTP_ROLE_PERMISSION table instead of TURBINE_ROLE_PERMISSION
     */
    @Override
    protected List<org.apache.fulcrum.security.torque.peer.TurbineRolePermissionPeerMapper> getTurbineRolePermissionsJoinTurbinePermission(Criteria criteria, Connection connection) throws TorqueException {
        if (criteria == null) {
            criteria = new Criteria();
        }
        criteria.where(com.uob.om.GtpRolePermissionPeer.ROLE_ID, this.getEntityId());
        
        // Use GtpRolePermissionPeer which queries GTP_ROLE_PERMISSION table
        // GtpRolePermission implements TurbineRolePermissionPeerMapper, so it's compatible
        List<GtpRolePermission> gtpRolePermissions;
        if (connection != null) {
            gtpRolePermissions = com.uob.om.GtpRolePermissionPeer.doSelectJoinGtpPermission(criteria, connection);
        } else {
            gtpRolePermissions = com.uob.om.GtpRolePermissionPeer.doSelectJoinGtpPermission(criteria);
        }
        
        // Convert List<GtpRolePermission> to List<TurbineRolePermissionPeerMapper>
        // Since GtpRolePermission implements TurbineRolePermissionPeerMapper, we can cast
        @SuppressWarnings("unchecked")
        List<org.apache.fulcrum.security.torque.peer.TurbineRolePermissionPeerMapper> result = (List<org.apache.fulcrum.security.torque.peer.TurbineRolePermissionPeerMapper>) (List<?>) gtpRolePermissions;
        return result;
    }

    /**
     * Override to use GtpUserGroupRolePeer instead of TurbineUserGroupRolePeer
     * This ensures queries use GTP_USER_GROUP_ROLE table instead of TURBINE_USER_GROUP_ROLE
     * 
     * Note: Using doSelect instead of doSelectJoinGtpGroup to avoid joined GtpGroup objects
     * being processed by maptoModel(), which expects only TurbineUserGroupRoleModelPeerMapper objects
     */
    protected List<TurbineUserGroupRole> getTurbineUserGroupRolesJoinTurbineGroup(Criteria criteria, Connection connection) throws TorqueException {
        if (criteria == null) {
            criteria = new Criteria();
        }
        criteria.where(com.uob.om.GtpUserGroupRolePeer.ROLE_ID, this.getEntityId());
        
        // Use doSelect instead of doSelectJoinGtpGroup to avoid joined GtpGroup objects
        // The framework will handle loading related objects separately if needed
        List<GtpUserGroupRole> gtpUserGroupRoles;
        if (connection != null) {
            gtpUserGroupRoles = com.uob.om.GtpUserGroupRolePeer.doSelect(criteria, connection);
        } else {
            gtpUserGroupRoles = com.uob.om.GtpUserGroupRolePeer.doSelect(criteria);
        }
        
        // Convert List<GtpUserGroupRole> to List<TurbineUserGroupRole>
        // Both implement TurbineUserGroupRoleModelPeerMapper, so we use unchecked cast
        // This is safe because both classes implement the same interface
        @SuppressWarnings("unchecked")
        List<TurbineUserGroupRole> result = (List<TurbineUserGroupRole>) (List<?>) gtpUserGroupRoles;
        return result;
    }

}

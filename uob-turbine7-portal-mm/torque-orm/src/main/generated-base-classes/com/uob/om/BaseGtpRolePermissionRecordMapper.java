package com.uob.om;

import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.HashSet;
import java.util.List;
import java.util.Set;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.apache.torque.Column;
import org.apache.torque.TorqueException;
import org.apache.torque.criteria.Criteria;
import org.apache.torque.om.mapper.RecordMapper;
import org.apache.torque.om.mapper.MappingStrategy;


/**
 * Maps ResultSet rows into GtpRolePermission objects.
 *
 * The skeleton for this class was autogenerated by Torque on:
 *
 * [Sat Jan 24 10:52:48 SGT 2026]
 *
 */
@SuppressWarnings("unused")
public class BaseGtpRolePermissionRecordMapper implements RecordMapper<GtpRolePermission>
{
    /** Serial version */
    private static final long serialVersionUID = 1769223168655L;

    /** The class log. */
    private static Log log
            = LogFactory.getLog(BaseGtpRolePermissionRecordMapper.class);
            
    private static final String ROLE_ID_EXPRESSION = BaseGtpRolePermissionPeer.ROLE_ID.getSqlExpression();
    private static final String PERMISSION_ID_EXPRESSION = BaseGtpRolePermissionPeer.PERMISSION_ID.getSqlExpression();

    private boolean useMappingStrategy = true;

    private MappingStrategy<GtpRolePermission> strategy;
    
    public void initStrategy()
    {
        this.strategy = new MappingStrategy<GtpRolePermission>();
    }
    

    /**
     * Constructs the object from the current row in the resultSet.
     *
     * @param resultSet the resultSet to operate on, already pointing
     *        to the correct row. Not null.
     * @param offset a possible offset in the columns to be considered
     *        (if previous columns contain other objects),
     *        or 0 for no offset.
     * @param criteria The criteria which created the result set.
     *        If set, the attributes to set in the data object
     *        are determined from the select columns in the criteria;
     *        if no matching column can be found, null is returned.
     *        If not set, all of the table's columns are read from the
     *        result set in the order defined in the table definition.
     *
     * @return the mapped object, not null.
     *
     * @throws TorqueException when reading fields from the RecordSet fails
     *         or if a Criteria is passed which contains select columns other
     *         than the columns in the GTP_ROLE_PERMISSION table.
     */
    public GtpRolePermission processRow(
                ResultSet resultSet,
                int offset,
                Criteria criteria)
            throws TorqueException
    {
        GtpRolePermission gtpRolePermission = new GtpRolePermission();

        try 
        {
            gtpRolePermission.setLoading(true);
            if (criteria == null)
            {
                gtpRolePermission.setRoleId(
                        getRoleId(resultSet, offset + 1));
                gtpRolePermission.setPermissionId(
                        getPermissionId(resultSet, offset + 2));
            }
            else
            {
                if (useMappingStrategy) {
                    initStrategy();
                }
                // try to get columns to be mapped
                // from criteria's select columns
                int totalOffset = offset + 1;
                List<Column> selectColumns = criteria.getSelectColumns();
                List<Column> columnsWithoutOffset = selectColumns.subList(
                        offset, 
                        selectColumns.size());
                Set<String> columnsMapped = new HashSet<String>();
                for (Column column : columnsWithoutOffset)
                {
                    final int nextOffset = totalOffset;                     if (!columnsMapped.contains(ROLE_ID_EXPRESSION ) && ROLE_ID_EXPRESSION.equals(
                            column.getSqlExpression()))
                    {
                        if (useMappingStrategy) 
                        {
                            strategy.addColumn(nextOffset, 
                                (res, inst) -> inst.setRoleId( getRoleId(res, nextOffset)));
                        } else
                        {
                            gtpRolePermission.setRoleId( getRoleId(resultSet, totalOffset));
                        }
                        columnsMapped.add( ROLE_ID_EXPRESSION );
                    }
                    else if (!columnsMapped.contains(PERMISSION_ID_EXPRESSION ) && PERMISSION_ID_EXPRESSION.equals(
                            column.getSqlExpression()))
                    {
                        if (useMappingStrategy) 
                        {
                            strategy.addColumn(nextOffset, 
                                (res, inst) -> inst.setPermissionId( getPermissionId(res, nextOffset)));
                        } else
                        {
                            gtpRolePermission.setPermissionId( getPermissionId(resultSet, totalOffset));
                        }
                        columnsMapped.add( PERMISSION_ID_EXPRESSION );
                    }
                    totalOffset++;
                }
                if (columnsMapped.isEmpty())
                {
                    log.debug("no columns to map found in criteria, "
                        + "returning null");
                    return null;
                }
                if (useMappingStrategy)
                {
                  this.strategy.finish(2, true);
                  this.strategy.execute(resultSet, gtpRolePermission);
                }
            }
            gtpRolePermission.setNew(false);
            gtpRolePermission.setModified(false);
        }
        finally
        {
            gtpRolePermission.setLoading(false);
        }
        return gtpRolePermission;
    }

    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field roleId in GtpRolePermission.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected int getRoleId(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            return resultSet.getInt(columnIndex);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field permissionId in GtpRolePermission.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected int getPermissionId(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            return resultSet.getInt(columnIndex);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }

}

package com.uob.om;

import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.HashSet;
import java.util.List;
import java.util.Set;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.apache.torque.Column;
import org.apache.torque.TorqueException;
import org.apache.torque.criteria.Criteria;
import org.apache.torque.om.mapper.RecordMapper;
import org.apache.torque.om.mapper.MappingStrategy;


/**
 * Maps ResultSet rows into GtpUser objects.
 *
 * The skeleton for this class was autogenerated by Torque on:
 *
 * [Sat Jan 24 10:52:48 SGT 2026]
 *
 */
@SuppressWarnings("unused")
public class BaseGtpUserRecordMapper implements RecordMapper<GtpUser>
{
    /** Serial version */
    private static final long serialVersionUID = 1769223168695L;

    /** The class log. */
    private static Log log
            = LogFactory.getLog(BaseGtpUserRecordMapper.class);
            
    private static final String USER_ID_EXPRESSION = BaseGtpUserPeer.USER_ID.getSqlExpression();
    private static final String LOGIN_NAME_EXPRESSION = BaseGtpUserPeer.LOGIN_NAME.getSqlExpression();
    private static final String PASSWORD_VALUE_EXPRESSION = BaseGtpUserPeer.PASSWORD_VALUE.getSqlExpression();
    private static final String FIRST_NAME_EXPRESSION = BaseGtpUserPeer.FIRST_NAME.getSqlExpression();
    private static final String LAST_NAME_EXPRESSION = BaseGtpUserPeer.LAST_NAME.getSqlExpression();
    private static final String EMAIL_EXPRESSION = BaseGtpUserPeer.EMAIL.getSqlExpression();
    private static final String CONFIRM_VALUE_EXPRESSION = BaseGtpUserPeer.CONFIRM_VALUE.getSqlExpression();
    private static final String MODIFIED_DATE_EXPRESSION = BaseGtpUserPeer.MODIFIED_DATE.getSqlExpression();
    private static final String CREATED_EXPRESSION = BaseGtpUserPeer.CREATED.getSqlExpression();
    private static final String LAST_LOGIN_EXPRESSION = BaseGtpUserPeer.LAST_LOGIN.getSqlExpression();
    private static final String OBJECTDATA_EXPRESSION = BaseGtpUserPeer.OBJECTDATA.getSqlExpression();

    private boolean useMappingStrategy = true;

    private MappingStrategy<GtpUser> strategy;
    
    public void initStrategy()
    {
        this.strategy = new MappingStrategy<GtpUser>();
    }
    

    /**
     * Constructs the object from the current row in the resultSet.
     *
     * @param resultSet the resultSet to operate on, already pointing
     *        to the correct row. Not null.
     * @param offset a possible offset in the columns to be considered
     *        (if previous columns contain other objects),
     *        or 0 for no offset.
     * @param criteria The criteria which created the result set.
     *        If set, the attributes to set in the data object
     *        are determined from the select columns in the criteria;
     *        if no matching column can be found, null is returned.
     *        If not set, all of the table's columns are read from the
     *        result set in the order defined in the table definition.
     *
     * @return the mapped object, not null.
     *
     * @throws TorqueException when reading fields from the RecordSet fails
     *         or if a Criteria is passed which contains select columns other
     *         than the columns in the GTP_USER table.
     */
    public GtpUser processRow(
                ResultSet resultSet,
                int offset,
                Criteria criteria)
            throws TorqueException
    {
        GtpUser gtpUser = new GtpUser();

        try 
        {
            gtpUser.setLoading(true);
            if (criteria == null)
            {
                gtpUser.setEntityId(
                        getEntityId(resultSet, offset + 1));
                gtpUser.setEntityName(
                        getEntityName(resultSet, offset + 2));
                gtpUser.setPassword(
                        getPassword(resultSet, offset + 3));
                gtpUser.setFirstName(
                        getFirstName(resultSet, offset + 4));
                gtpUser.setLastName(
                        getLastName(resultSet, offset + 5));
                gtpUser.setEmail(
                        getEmail(resultSet, offset + 6));
                gtpUser.setConfirmed(
                        getConfirmed(resultSet, offset + 7));
                gtpUser.setModifiedDate(
                        getModifiedDate(resultSet, offset + 8));
                gtpUser.setCreateDate(
                        getCreateDate(resultSet, offset + 9));
                gtpUser.setLastLogin(
                        getLastLogin(resultSet, offset + 10));
                gtpUser.setObjectdata(
                        getObjectdata(resultSet, offset + 11));
            }
            else
            {
                if (useMappingStrategy) {
                    initStrategy();
                }
                // try to get columns to be mapped
                // from criteria's select columns
                int totalOffset = offset + 1;
                List<Column> selectColumns = criteria.getSelectColumns();
                List<Column> columnsWithoutOffset = selectColumns.subList(
                        offset, 
                        selectColumns.size());
                Set<String> columnsMapped = new HashSet<String>();
                for (Column column : columnsWithoutOffset)
                {
                    final int nextOffset = totalOffset;                     if (!columnsMapped.contains(USER_ID_EXPRESSION ) && USER_ID_EXPRESSION.equals(
                            column.getSqlExpression()))
                    {
                        if (useMappingStrategy) 
                        {
                            strategy.addColumn(nextOffset, 
                                (res, inst) -> inst.setEntityId( getEntityId(res, nextOffset)));
                        } else
                        {
                            gtpUser.setEntityId( getEntityId(resultSet, totalOffset));
                        }
                        columnsMapped.add( USER_ID_EXPRESSION );
                    }
                    else if (!columnsMapped.contains(LOGIN_NAME_EXPRESSION ) && LOGIN_NAME_EXPRESSION.equals(
                            column.getSqlExpression()))
                    {
                        if (useMappingStrategy) 
                        {
                            strategy.addColumn(nextOffset, 
                                (res, inst) -> inst.setEntityName( getEntityName(res, nextOffset)));
                        } else
                        {
                            gtpUser.setEntityName( getEntityName(resultSet, totalOffset));
                        }
                        columnsMapped.add( LOGIN_NAME_EXPRESSION );
                    }
                    else if (!columnsMapped.contains(PASSWORD_VALUE_EXPRESSION ) && PASSWORD_VALUE_EXPRESSION.equals(
                            column.getSqlExpression()))
                    {
                        if (useMappingStrategy) 
                        {
                            strategy.addColumn(nextOffset, 
                                (res, inst) -> inst.setPassword( getPassword(res, nextOffset)));
                        } else
                        {
                            gtpUser.setPassword( getPassword(resultSet, totalOffset));
                        }
                        columnsMapped.add( PASSWORD_VALUE_EXPRESSION );
                    }
                    else if (!columnsMapped.contains(FIRST_NAME_EXPRESSION ) && FIRST_NAME_EXPRESSION.equals(
                            column.getSqlExpression()))
                    {
                        if (useMappingStrategy) 
                        {
                            strategy.addColumn(nextOffset, 
                                (res, inst) -> inst.setFirstName( getFirstName(res, nextOffset)));
                        } else
                        {
                            gtpUser.setFirstName( getFirstName(resultSet, totalOffset));
                        }
                        columnsMapped.add( FIRST_NAME_EXPRESSION );
                    }
                    else if (!columnsMapped.contains(LAST_NAME_EXPRESSION ) && LAST_NAME_EXPRESSION.equals(
                            column.getSqlExpression()))
                    {
                        if (useMappingStrategy) 
                        {
                            strategy.addColumn(nextOffset, 
                                (res, inst) -> inst.setLastName( getLastName(res, nextOffset)));
                        } else
                        {
                            gtpUser.setLastName( getLastName(resultSet, totalOffset));
                        }
                        columnsMapped.add( LAST_NAME_EXPRESSION );
                    }
                    else if (!columnsMapped.contains(EMAIL_EXPRESSION ) && EMAIL_EXPRESSION.equals(
                            column.getSqlExpression()))
                    {
                        if (useMappingStrategy) 
                        {
                            strategy.addColumn(nextOffset, 
                                (res, inst) -> inst.setEmail( getEmail(res, nextOffset)));
                        } else
                        {
                            gtpUser.setEmail( getEmail(resultSet, totalOffset));
                        }
                        columnsMapped.add( EMAIL_EXPRESSION );
                    }
                    else if (!columnsMapped.contains(CONFIRM_VALUE_EXPRESSION ) && CONFIRM_VALUE_EXPRESSION.equals(
                            column.getSqlExpression()))
                    {
                        if (useMappingStrategy) 
                        {
                            strategy.addColumn(nextOffset, 
                                (res, inst) -> inst.setConfirmed( getConfirmed(res, nextOffset)));
                        } else
                        {
                            gtpUser.setConfirmed( getConfirmed(resultSet, totalOffset));
                        }
                        columnsMapped.add( CONFIRM_VALUE_EXPRESSION );
                    }
                    else if (!columnsMapped.contains(MODIFIED_DATE_EXPRESSION ) && MODIFIED_DATE_EXPRESSION.equals(
                            column.getSqlExpression()))
                    {
                        if (useMappingStrategy) 
                        {
                            strategy.addColumn(nextOffset, 
                                (res, inst) -> inst.setModifiedDate( getModifiedDate(res, nextOffset)));
                        } else
                        {
                            gtpUser.setModifiedDate( getModifiedDate(resultSet, totalOffset));
                        }
                        columnsMapped.add( MODIFIED_DATE_EXPRESSION );
                    }
                    else if (!columnsMapped.contains(CREATED_EXPRESSION ) && CREATED_EXPRESSION.equals(
                            column.getSqlExpression()))
                    {
                        if (useMappingStrategy) 
                        {
                            strategy.addColumn(nextOffset, 
                                (res, inst) -> inst.setCreateDate( getCreateDate(res, nextOffset)));
                        } else
                        {
                            gtpUser.setCreateDate( getCreateDate(resultSet, totalOffset));
                        }
                        columnsMapped.add( CREATED_EXPRESSION );
                    }
                    else if (!columnsMapped.contains(LAST_LOGIN_EXPRESSION ) && LAST_LOGIN_EXPRESSION.equals(
                            column.getSqlExpression()))
                    {
                        if (useMappingStrategy) 
                        {
                            strategy.addColumn(nextOffset, 
                                (res, inst) -> inst.setLastLogin( getLastLogin(res, nextOffset)));
                        } else
                        {
                            gtpUser.setLastLogin( getLastLogin(resultSet, totalOffset));
                        }
                        columnsMapped.add( LAST_LOGIN_EXPRESSION );
                    }
                    else if (!columnsMapped.contains(OBJECTDATA_EXPRESSION ) && OBJECTDATA_EXPRESSION.equals(
                            column.getSqlExpression()))
                    {
                        if (useMappingStrategy) 
                        {
                            strategy.addColumn(nextOffset, 
                                (res, inst) -> inst.setObjectdata( getObjectdata(res, nextOffset)));
                        } else
                        {
                            gtpUser.setObjectdata( getObjectdata(resultSet, totalOffset));
                        }
                        columnsMapped.add( OBJECTDATA_EXPRESSION );
                    }
                    totalOffset++;
                }
                if (columnsMapped.isEmpty())
                {
                    log.debug("no columns to map found in criteria, "
                        + "returning null");
                    return null;
                }
                if (useMappingStrategy)
                {
                  this.strategy.finish(11, true);
                  this.strategy.execute(resultSet, gtpUser);
                }
            }
            gtpUser.setNew(false);
            gtpUser.setModified(false);
        }
        finally
        {
            gtpUser.setLoading(false);
        }
        return gtpUser;
    }

    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field entityId in GtpUser.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected Integer getEntityId(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            Integer value = resultSet.getInt(columnIndex);
            if (resultSet.wasNull())
            {
                value = null;
            }
            return value;
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field entityName in GtpUser.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected String getEntityName(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            return resultSet.getString(columnIndex);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field password in GtpUser.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected String getPassword(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            return resultSet.getString(columnIndex);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field firstName in GtpUser.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected String getFirstName(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            return resultSet.getString(columnIndex);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field lastName in GtpUser.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected String getLastName(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            return resultSet.getString(columnIndex);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field email in GtpUser.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected String getEmail(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            return resultSet.getString(columnIndex);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field confirmed in GtpUser.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected String getConfirmed(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            return resultSet.getString(columnIndex);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field modifiedDate in GtpUser.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected java.util.Date getModifiedDate(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            java.util.Date value = resultSet.getTimestamp(columnIndex);
            if (resultSet.wasNull())
            {
                value = null;
            }
            return value;
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field createDate in GtpUser.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected java.util.Date getCreateDate(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            java.util.Date value = resultSet.getTimestamp(columnIndex);
            if (resultSet.wasNull())
            {
                value = null;
            }
            return value;
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field lastLogin in GtpUser.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected java.util.Date getLastLogin(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            java.util.Date value = resultSet.getTimestamp(columnIndex);
            if (resultSet.wasNull())
            {
                value = null;
            }
            return value;
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }
    /**
     * Reads the value of the <code>columnIndex</code>'th column
     * in the <code>resultSet</code> so that it can be used to set
     * the field objectdata in GtpUser.
     *
     * @param resultSet the ResultSet to read from, not null.
     * @param columnIndex the index in the resultSet which should be read.
     *
     * @return the content of the column.
     *
     * @throws SQLException if a problem occurs when reading
     *         from the resultSet.
     */
    protected byte[] getObjectdata(
                ResultSet resultSet,
                int columnIndex)
            throws TorqueException
    {
        try
        {
            byte[] value = resultSet.getBytes(columnIndex);
            if (resultSet.wasNull())
            {
                value = null;
            }
            return value;
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
    }

}
